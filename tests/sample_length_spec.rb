require 'rspec'
require_relative '../helpers/samplecalc.rb'

include SampleCalc
include Math

describe "Calculates samples for given beats at given bpm" do
  it '4 beats 130 bpm' do
     = SampleCalc.samples_per_beats
    expect().to eq)
  end
  it '4 beats 130 bpm' do
     = SampleCalc.samples_per_beats
    expect().to eq)
  end
end
